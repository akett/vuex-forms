import {
    alpha,
    alphaNum,
    and,
    between,
    email,
    maxLength,
    minLength,
    numeric,
    or,
    required,
    requiredIf,
    requiredUnless,
    sameAs,
    url
} from "vuelidate/lib/validators"

import accepted from "./accepted"
import alphaDash from "./alphaDash"

export {
    accepted,
    alpha,
    alphaDash,
    alphaNum,
    and,
    between,
    email,
    maxLength,
    minLength,
    numeric,
    or,
    required,
    requiredIf,
    requiredUnless,
    sameAs,
    url
}